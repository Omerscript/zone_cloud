# 🌩️ ZONE | Cloud — دليل الربط (نسخة المستخدم)

**حقوق المطور:** @IEI_T

**رابط السحابة:**

```text
https://supertutelary-soberly-ezra.ngrok-free.dev
```


يمكك الان تخزين بشكل لان نهائي **
> فقط اربط سكربتك أو بوتك الحالي لتخزين واسترجاع الملفات.
> كل مستخدم يمكنه رفع الملفات باستخدام اسم مستخدم وكلمة مرور، ليبقى ملفاته خاصة ولا يمكن للمستخدمين الآخرين الوصول إليها.

---

## 📌 المكتبة المطلوبة

لتوصيل سكربتك أو بوتك بالسحابة:

```bash
pip install requests
```

> المكتبة المطلوبة فقط هي `requests`.

---

## 🔗 واجهات الـ API

استخدم الرابط التالي في سكربتك:

```python
CLOUD_API = "[https://supertutelary-soberly-ezra.ngrok-free.dev](https://supertutelary-soberly-ezra.ngrok-free.dev)"
API_KEY = "CLOUDEZONE:512523524952740985RHIEWHRK3HK423O23IIO23U325"
username = "ضع اسم المستخدم هنا"
password = "ضع كلمة المرور هنا"
```

| الواجهة     | الطريقة | الوصف                                          |
| ----------- | ------- | ---------------------------------------------- |
| `/upload`   | POST    | رفع ملف **باستخدام اسم المستخدم وكلمة المرور** |
| `/download` | GET     | تحميل ملف **بتحديد اسم المستخدم وكلمة المرور** |
| `/list`     | GET     | عرض جميع الملفات لمستخدم **محدد**              |
| `/delete`   | DELETE  | حذف ملف لمستخدم **محدد**                       |
| `/status`   | GET     | التحقق من حالة السحابة                         |

---

## 📝 كيفية ربط سكربتك أو بوتك

1. **تثبيت مكتبة `requests`**
   تأكد أن سكربتك أو بوتك يحتوي على المكتبة مثبتة.

2. **تعيين رابط السحابة ومفتاح API**
   ضع رابط السحابة ومفتاح الـ API كمتغيرات داخل سكربتك.

3. **تسجيل دخول المستخدم**
   يجب على كل مستخدم إدخال `username` و `password` عند كل طلب.
   الملفات مخزنة لكل مستخدم على حدة، لذلك لا يمكن للمستخدم الوصول إلى ملفات الآخرين.

4. **رفع الملفات إلى السحابة**
   استخدم طلب POST إلى `/upload` مع:

   * `username`
   * `password`
   * `key` (API_KEY)
   * `file`

5. **تحميل الملفات من السحابة**
   استخدم طلب GET إلى `/download` مع:

   * `filename`
   * `username`
   * `password`
   * `key` (API_KEY)

6. **عرض الملفات المخزنة لمستخدم**
   استخدم طلب GET إلى `/list` مع:

   * `username`
   * `password`
   * `key` (API_KEY)

7. **حذف الملفات**
   استخدم طلب DELETE إلى `/delete` مع:

   * `filename`
   * `username`
   * `password`
   * `key` (API_KEY)

8. **فحص حالة السحابة (اختياري)**
   استخدم طلب GET إلى `/status` للتحقق من أن السحابة متصلة.

---

## ⚡ فوائد ربط بوتك أو سكربتك بالسحابة

* يعمل مباشرة مع بوت Telegram أو سكربت Python الحالي.
* كل مستخدم لديه **تخزين ملفات خاص** باستخدام اسم المستخدم وكلمة المرور.
* لا حاجة لاستضافة سيرفر خاص بك.
* تخزين ملفات بلا حدود.
* يمكن لعدة مستخدمين رفع ملفات بنفس الاسم بأمان (بدون تعارض).
* دمج سهل باستخدام مكتبة `requests` فقط.

---

**المطور:** ZONE
**حساب Telegram:** @IEI_T

---

إذا أحببت، أقدر أسوي لك نسخة جاهزة **Markdown** بالكامل بالعربي لتضعها مباشرة في README بدون أي تعديل. هل تريد أن أفعل ذلك؟
