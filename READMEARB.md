# 🌩️ ZONE | CLOUD

**رابط السحابة:**
إذا حدث خطأ، قد يكون الرابط تغير. يرجى التواصل مع المطور.

[https://supertutelary-soberly-ezra.ngrok-free.dev](https://supertutelary-soberly-ezra.ngrok-free.dev)

> هذه السحابة تعمل بالفعل. **لا تحتاج** لإنشاء بوت أو سيرفر.
> كل ما عليك هو ربط سكربتك أو بوتك لتخزين واسترجاع الملفات.

---

## 📌 المكتبة المطلوبة

لربط سكربتك أو بوتك بالسحابة:

```bash
pip install requests
```

> فقط `requests` مطلوبة. لا حاجة لـ FastAPI أو Uvicorn أو ngrok.

---

## 🔗 نقاط النهاية (API Endpoints)

استخدم الرابط التالي داخل سكربتك:

```python
CLOUD_API = "https://supertutelary-soberly-ezra.ngrok-free.dev"
API_KEY = "supersecret123"
```

| نقطة النهاية           | الطريقة | الوصف                  |
| ---------------------- | ------- | ---------------------- |
| `/upload`              | POST    | رفع ملف                |
| `/download/{filename}` | GET     | تنزيل ملف              |
| `/list`                | GET     | عرض كل الملفات المخزنة |
| `/delete/{filename}`   | DELETE  | حذف ملف                |
| `/status`              | GET     | التحقق من حالة السحابة |

---

## 📝 طريقة ربط سكربتك أو بوتك

1. **تثبيت `requests`**
   يجب أن يحتوي سكربتك أو بوتك على مكتبة `requests`.

2. **تعيين رابط السحابة ومفتاح الـ API**
   عيّن رابط السحابة ومفتاح الـ API كمتغيرات في سكربتك.

3. **رفع الملفات إلى السحابة**
   استخدم POST إلى `/upload` مع مفتاح الـ API والملف الذي تريد تخزينه.

4. **تنزيل الملفات من السحابة**
   استخدم GET إلى `/download/{filename}` مع مفتاح الـ API لجلب الملفات.

5. **عرض الملفات المخزنة**
   استخدم GET إلى `/list` مع مفتاح الـ API لرؤية كل الملفات في السحابة.

6. **حذف الملفات**
   استخدم DELETE إلى `/delete/{filename}` مع مفتاح الـ API لإزالة أي ملف.

7. **اختياري: التحقق من صحة السحابة**
   استخدم GET إلى `/status` للتأكد أن السحابة تعمل.

---

## ⚡ فوائد ربط بوتك أو سكربتك

* يعمل فورًا مع بوتات أو أي سكربت بايثون (أو أي لغة أخرى).
* لا حاجة لاستضافة سيرفر خاص بك.
* تخزين ملفات غير محدود.
* خصوصية الملفات عالية.

---

**المطور:** ZONE
**حساب تيليجرام:** @IEI_T
